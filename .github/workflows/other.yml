name: Test
on:
  workflow_call:
    secrets:
      gh_token:
        required: true

jobs:
  test:
    if: "!contains(github.event.head_commit.message, '[skip test]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.gh_token }}

      - name: Install Bun
        uses: oven-sh/setup-bun@v1

      - name: Run tests
        shell: bash
        run: bun run test test
